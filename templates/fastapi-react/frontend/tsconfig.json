{
    "compilerOptions": {
      "target": "es5",
      "lib": ["dom", "dom.iterable", "esnext"],
      "allowJs": true,
      "skipLibCheck": true,
      "esModuleInterop": true,
      "allowSyntheticDefaultImports": true,
      "strict": true,
      "forceConsistentCasingInFileNames": true,
      "noFallthroughCasesInSwitch": true,
      "module": "esnext",
      "moduleResolution": "node",
      "resolveJsonModule": true,
      "isolatedModules": true,
      "noEmit": true,
      "jsx": "react-jsx"
    },
    "include": ["src"]
  }
  
  // Instructions for modifying this file when breaking from the template:
  // 1. If you need to support additional JavaScript features, add them to the "lib" array.
  // 2. If you want to change the TypeScript target version, modify the "target" field.
  // 3. If you need to adjust module resolution, modify the "moduleResolution" field.
  // 4. If you want to emit declaration files, set "declaration" to true and specify an "outDir".
  // 5. If you need to include or exclude specific files, use the "include" and "exclude" arrays.
  // 6. Always test your application after making changes to ensure everything still works as expected.